# Dependency major-version upgrade notes

Researched on 2026-08-19 from the initial `pnpm outdated --format json` output. "Current" below means the installed version reported by pnpm before the upgrade work began. Targets are the registry's `latest` versions from that run.

## Compatibility gates

| Gate          | Action for this repository                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Source                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Node.js       | Use Node 22.22.1 or newer for the complete target set. This satisfies Next 16, ESLint 10, Babel 8, Wretch 3, Octokit 5, and lint-staged 17. The local Node 24.19.0 also satisfies them.                                                                                                                                                                                                                                                                                             | [Next 16 requirements](https://nextjs.org/docs/app/guides/upgrading/version-16), [ESLint 10 migration](https://eslint.org/docs/latest/use/migrate-to-10.0.0), [Babel 8 migration](https://babeljs.io/docs/v8-migration/), [lint-staged migration](https://github.com/lint-staged/lint-staged/blob/main/MIGRATION.md), [Wretch migration](https://github.com/elbywan/wretch/blob/master/MIGRATION_V2_V3.md) |
| TypeScript    | Do not install TypeScript 7 as the repository's direct `typescript` package yet. `@typescript-eslint/*@8.67.0` supports TypeScript `<6.1.0`, and `ts-jest@29.4.12` declares `<7`. TypeScript 7 deliberately ships without a compiler API. The TypeScript team documents a side-by-side TypeScript 6 compatibility alias for tools that need the API. Keep TypeScript 6 as `typescript`, or use the documented dual-package arrangement if TypeScript 7 CLI performance is required. | [TypeScript 7 release](https://devblogs.microsoft.com/typescript/announcing-typescript-7-0/), [typescript-eslint support range](https://typescript-eslint.io/users/dependency-versions/), [ts-jest changelog](https://github.com/kulshekhar/ts-jest/blob/main/CHANGELOG.md)                                                                                                                                |
| Babel         | `@babel/preset-env@8` requires `@babel/core@8`, but `ts-jest@29.4.12` requires `@babel/core <8`. This repository's active Jest preset is `ts-jest`; `babel-jest` and `@babel/preset-env` are not referenced. Remove the unused Babel packages, or hold the preset at 7 rather than introducing conflicting Babel cores.                                                                                                                                                             | [Babel 8 migration](https://babeljs.io/docs/v8-migration/), [ts-jest package/changelog](https://github.com/kulshekhar/ts-jest/blob/main/CHANGELOG.md)                                                                                                                                                                                                                                                      |
| Framework set | Upgrade `next`, `eslint-config-next`, `react`, `react-dom`, and `@types/react` together. Next 16 accepts React 18 or 19 at the package level, but the Next upgrade path and the request to update everything lead to React 19.                                                                                                                                                                                                                                                      | [Next 16 guide](https://nextjs.org/docs/app/guides/upgrading/version-16), [React 19 guide](https://react.dev/blog/2024/04/25/react-19-upgrade-guide)                                                                                                                                                                                                                                                       |
| Lint set      | ESLint 10 only supports flat config. Replace `.eslintrc.json`, invoke `eslint` directly, and remove every `next lint` call. `eslint-config-next@16` is a flat-config array and supports ESLint 9 or newer.                                                                                                                                                                                                                                                                          | [ESLint 10 migration](https://eslint.org/docs/latest/use/migrate-to-10.0.0), [Next ESLint setup](https://nextjs.org/docs/app/api-reference/config/eslint)                                                                                                                                                                                                                                                  |
| Test set      | Jest 30 works with `ts-jest@29.4.12`, which added Jest 30 support in 29.4.0. Pair it with `@types/jest@30`. Jest 30 needs TypeScript 5.4 or newer.                                                                                                                                                                                                                                                                                                                                  | [Jest 30 migration](https://jestjs.io/docs/upgrading-to-jest30), [ts-jest changelog](https://github.com/kulshekhar/ts-jest/blob/main/CHANGELOG.md)                                                                                                                                                                                                                                                         |

## Actionable major-upgrade matrix

| Packages                                                                                            | Current -> target                                                             | Relevant changes and repository action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `next`, `eslint-config-next`                                                                        | 13.5.4 -> 16.3.0                                                              | Run the official Next codemod. Convert `params` in both `app/transcription/[language]/page.tsx` and `app/transcription/[language]/[slug]/page.tsx` to promises and await them. Next 16 removes synchronous request API access. In the slug page, `generateStaticParams()` currently returns `{ language, text: text.slug }`; correct the second key to `slug`. Remove `swcMinify` from `next.config.js`; Next 15 removed that option. Replace `next lint` in `package.json` and `.lintstagedrc.js` with the ESLint CLI. Turbopack is now the default build system. Review the Next 15 cache-default changes for server fetches and route handlers. Sources: [Next 15](https://nextjs.org/docs/app/guides/upgrading/version-15), [Next 16](https://nextjs.org/docs/app/guides/upgrading/version-16), [Next compiler](https://nextjs.org/docs/architecture/nextjs-compiler). |
| `react`, `react-dom`, `@types/react`                                                                | 18.2.0 -> 19.2.8; types 18.3.27 -> 19.2.18                                    | Use the React 19 codemods, including the type codemod. The modern JSX transform is required. React removes legacy APIs and changes error reporting. Type changes include required `useRef` arguments, mutable refs, `ReactElement.props` defaulting to `unknown`, scoped `React.JSX`, and stricter ref callbacks. The current `useRef` calls already pass initial values. Replace global `JSX.Element` references in `NavbarLink.tsx`, `PrivateLayout.tsx`, and `ResultDisplay.tsx` with `React.JSX.Element`, `React.ReactNode`, or inferred types. No removed runtime React APIs were found. Source: [React 19 upgrade guide](https://react.dev/blog/2024/04/25/react-19-upgrade-guide).                                                                                                                                                                                  |
| `eslint`, `@typescript-eslint/eslint-plugin`, `@typescript-eslint/parser`, `eslint-config-prettier` | 8.51.0 -> 10.8.1; TS ESLint 6.21.0 -> 8.67.0; Prettier config 9.1.2 -> 10.1.8 | Replace `.eslintrc.json` with `eslint.config.mjs`. ESLint 10 rejects eslintrc files and requires Node 20.19+, 22.13+, or 24+. Use the Next flat configs, then append Prettier last. Preserve the existing `import/order` rule. typescript-eslint v8 supports ESLint 10 but not TypeScript 7. Its recommended rules changed, so review new lint failures rather than disabling the preset wholesale. Sources: [ESLint 10](https://eslint.org/docs/latest/use/migrate-to-10.0.0), [typescript-eslint v8](https://typescript-eslint.io/blog/announcing-typescript-eslint-v8/), [eslint-config-prettier changelog](https://github.com/prettier/eslint-config-prettier/blob/main/CHANGELOG.md).                                                                                                                                                                                 |
| `jest`, `babel-jest`, `@types/jest`                                                                 | 29.7.0 -> 30.4.2; 29.7.0 -> 30.4.1; types 29.5.14 -> 30.0.0                   | The current tests do not use removed matcher aliases, custom sequencers, internal Jest imports, or the renamed path-pattern flag. Run the suite for changed equality behavior and stricter `CalledWith` types. Keep `ts-jest@29.4.12`, which supports Jest 30. Remove `babel-jest` if it remains unused; otherwise coordinate its Babel core version with the Babel decision above. Source: [Jest 30 migration](https://jestjs.io/docs/upgrading-to-jest30).                                                                                                                                                                                                                                                                                                                                                                                                               |
| `typescript`                                                                                        | 5.9.3 -> 7.0.2                                                                | Do not make 7.0.2 the direct toolchain version while `typescript-eslint` and `ts-jest` import the compiler API. Independently, this `tsconfig.json` must change before TS 7: `target: "es5"` is removed and `moduleResolution: "node"` is removed. Use at least ES2015 and a supported mode such as `bundler`, in line with Next's generated config. TS 7 also defaults `types` to `[]`, so list `node` and `jest` if global types disappear. Source: [TypeScript 7 release](https://devblogs.microsoft.com/typescript/announcing-typescript-7-0/).                                                                                                                                                                                                                                                                                                                        |
| `@babel/preset-env`                                                                                 | 7.28.5 -> 8.0.2                                                               | Babel 8 is ESM-only, requires Node `^22.18.0 \| >=24.11.0`, changes the default target to `defaults`, and requires `@babel/core@8`. There is no Babel config in this repo and the preset is unused. Removing it is lower risk than activating a second transformer beside Next SWC and ts-jest. Source: [Babel 8 migration](https://babeljs.io/docs/v8-migration/).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `tailwindcss`                                                                                       | 3.4.19 -> 4.3.3                                                               | This is a real stylesheet migration. Install `@tailwindcss/postcss`; remove `autoprefixer` from PostCSS; replace the three `@tailwind` directives with `@import "tailwindcss"`; move or explicitly load the JS config. Tailwind 4 is not designed to run inside Sass, so split the Tailwind entry out of `src/styles/app.scss` while retaining Sass files separately. Migrate removed opacity utilities such as `bg-opacity-*` and `ring-opacity-*` to slash opacity, and review renamed `shadow`, `rounded`, ring, and outline utilities. Browser floor is Safari 16.4, Chrome 111, Firefox 128. Source: [Tailwind 4 upgrade guide](https://tailwindcss.com/docs/upgrade-guide).                                                                                                                                                                                          |
| `@headlessui/react`                                                                                 | 1.7.19 -> 2.2.10                                                              | Replace removed `Dialog.Overlay` in `FeedbackModal.tsx` with a regular backdrop and `DialogPanel`. Prefer explicit component imports over deprecated dot notation. `Transition`, Listbox options, and option default elements changed. Dropdowns are modal by default, so verify scroll locking in `FeedbackSelect.tsx`; pass `modal={false}` only if the old non-modal behavior is intended. The `active` render prop is deprecated in favor of `focus`. Source: [Headless UI v2 release and migration](https://github.com/tailwindlabs/headlessui/releases/tag/%40headlessui%2Freact%40v2.0.0).                                                                                                                                                                                                                                                                          |
| `@sendgrid/mail`                                                                                    | 7.7.0 -> 8.1.6                                                                | Version 8 drops Node versions below 12 and upgrades Axios. The repository's `setApiKey`, `send`, and `MailDataRequired` usage has no documented API migration. Source: [SendGrid 8 upgrade guide](https://github.com/sendgrid/sendgrid-nodejs/blob/8.0.0/UPGRADE.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `dotenv`                                                                                            | 16.6.1 -> 17.4.2                                                              | Version 17 logs injected file/key information by default. Add `quiet: true` to the three `dotenv.config()` calls if test/script output should remain quiet. No parsing/API migration is documented. Source: [dotenv changelog](https://github.com/motdotla/dotenv/blob/master/CHANGELOG.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `jspdf`                                                                                             | 2.5.2 -> 4.2.1                                                                | Version 3 dropped Internet Explorer and otherwise kept the API. Version 4 blocks file-system reads by default to fix a path traversal issue and states that it has no other breaking changes. The browser-side `new jsPDF()`, font, image, text, and save calls should remain valid; test actual PDF generation. Sources: [jsPDF 3 release](https://github.com/parallax/jsPDF/releases/tag/v3.0.0), [jsPDF 4 advisory](https://github.com/parallax/jsPDF/security/advisories/GHSA-f8cm-6447-x5h2).                                                                                                                                                                                                                                                                                                                                                                         |
| `octokit`                                                                                           | 3.2.2 -> 5.0.5                                                                | Version 4 made the package ESM-only and removed deprecated OAuth authentication. Version 5 requires Node 20. The current `import { Octokit }` and token auth are supported, but the server bundle must consume ESM correctly. Source: [Octokit 4 release](https://github.com/octokit/octokit.js/releases/tag/v4.0.0), [Octokit 5 release](https://github.com/octokit/octokit.js/releases/tag/v5.0.0).                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `react-dropdown`                                                                                    | 1.11.0 -> 2.0.0                                                               | React 18 or 19 is required. The package now uses an exports map, a button trigger, semantic list markup, controlled `null`, and normalized option results. The three editor dropdowns use class selectors and standard option callbacks, but test focus, keyboard interaction, and styles because `.Dropdown-control` is now a button. Add an accessible name with `aria-label` or `aria-labelledby` to each instance. Sources: [official migration guide](https://github.com/fraserxu/react-dropdown/blob/master/README.md#migrating-from-1x), [2.0.0 changelog](https://github.com/fraserxu/react-dropdown/blob/master/CHANGELOG.md#200).                                                                                                                                                                                                                                |
| `supabase` CLI                                                                                      | 1.226.4 -> 2.115.0                                                            | The v2 announcement says the major itself has no breaking changes. No Supabase CLI project directory or CLI scripts exist in this repository. Upgrade the binary package and smoke-test `supabase --version`. If the CLI becomes part of the workflow, initialize and commit `supabase/config.toml`, pin the CLI version, and test migrations/generated types because Supabase does not guarantee those outputs across service image changes even within a CLI major. Sources: [Supabase CLI v2 announcement](https://supabase.com/blog/cli-v2-config-as-code#breaking-changes), [Supabase CLI repository](https://github.com/supabase/cli), [local workflow](https://supabase.com/docs/guides/local-development/cli-workflows).                                                                                                                                           |
| `wretch`                                                                                            | 2.11.1 -> 3.0.9                                                               | Requires Node 22. It removes polyfill/global configuration APIs, replaces `errorType` with `customError`, changes retry defaults, and changes addon option signatures. This repo uses only `wretch(url).post(...).json()`, so no call-site migration is expected. Source: [Wretch v2 to v3 migration](https://github.com/elbywan/wretch/blob/master/MIGRATION_V2_V3.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `zod`                                                                                               | 3.25.76 -> 4.4.3                                                              | Most schemas are compatible. `z.Schema` remains as a deprecated compatibility alias, so replace the two generic constraints in `src/util/typeUtils.ts` with `z.ZodType`. `ZodError.flatten()` used in `pages/api/search/words.ts` is also deprecated; move to `z.treeifyError()` or preserve the old output through `z.flattenError()` after checking the desired response shape. Error text and precedence changed, so API tests should not assume v3 messages. Source: [Zod 4 migration](https://zod.dev/v4/changelog).                                                                                                                                                                                                                                                                                                                                                  |
| `zustand`                                                                                           | 4.5.7 -> 5.0.15                                                               | React 18 and TypeScript 4.5 are the minimums; ES5 and default exports are gone. Zustand 5 can loop when a selector returns a fresh object. Five selectors have this pattern: one each in `ResultDisplay.tsx`, `ResultEditButton.tsx`, and `TranscriptionEditorOptions.tsx`, plus two in `TranscriptionActionButtons.tsx`. Use `useShallow` or select fields separately. Review the custom `persist as unknown as QuotaStorePersist` cast and persisted-state behavior, since v5 changes persist middleware and tightens `setState(..., true)` types. Source: [Zustand v5 migration](https://zustand.docs.pmnd.rs/reference/migrations/migrating-to-v5).                                                                                                                                                                                                                    |
| `husky`                                                                                             | 8.0.3 -> 9.1.7                                                                | Change `prepare` from `husky install` to `husky`. Remove the two deprecated bootstrap lines from `.husky/pre-commit` and `.husky/post-merge`; v10 will reject them. Source: [Husky setup](https://typicode.github.io/husky/how-to.html), [Husky v9 release notes](https://github.com/typicode/husky/releases).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `lint-staged`                                                                                       | 14.0.1 -> 17.3.0                                                              | Requires Node 22.22.1 and Git 2.32. Version 16 removed `--shell`, which this repo does not use. The actual repo migration is the lint command: `.lintstagedrc.js` currently builds a removed `next lint --fix --file ...` invocation and must build an `eslint --fix` invocation instead. Source: [lint-staged migration](https://github.com/lint-staged/lint-staged/blob/main/MIGRATION.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `@types/node`                                                                                       | 20.19.27 -> 26.2.0                                                            | Match the declaration major to the deployed Node runtime when practical. This is a type-model change, not a runtime upgrade. Because the full dependency set already needs modern Node, moving to Node 24 plus its matching type line is a more conservative deployment choice than claiming Node 26 APIs are available. DefinitelyTyped package majors track the corresponding library major and can include its breaking API changes. Source: [DefinitelyTyped versioning](https://github.com/DefinitelyTyped/DefinitelyTyped#how-do-definitely-typed-package-versions-relate-to-versions-of-the-corresponding-library).                                                                                                                                                                                                                                                 |

## Same-major upgrade that still needs migration work

`@supabase/supabase-js` moves from 2.38.0 to 2.112.3, so it does not qualify as a major-version change, but it changes the structural contract expected from the generated `Database` type. Regenerate the schema with the updated CLI instead of maintaining it by hand. The current generated file needs `Relationships` on every table and a schema-level `CompositeTypes` member for current PostgREST generics. After regeneration, remove avoidable `@ts-expect-error` comments and run both `tsc` and the sitemap build path. Supabase documents `supabase gen types --lang typescript --linked` and `--local` in its [local development workflow](https://supabase.com/docs/guides/local-development/cli-workflows#generating-types).

Status on 2026-08-19: regeneration was attempted with `pnpm exec supabase gen types typescript --project-id uapxcccwilonmeafingm --schema public`, but the CLI requires `supabase login` or `SUPABASE_ACCESS_TOKEN`. The REST schema endpoint also requires a service-role key. Until an authenticated regeneration succeeds, the empty `Relationships` arrays in `src/schema.ts` are compatibility placeholders, not verified schema metadata.

## Recommended order

1. Establish Node 22.22.1 or Node 24 in CI/deployment and add the matching `engines.node` declaration.
2. Upgrade Next, React, and React types. Apply the Next and React codemods, then fix async route params and Headless UI.
3. Migrate ESLint to flat config and direct CLI usage. Update Husky and lint-staged at the same time.
4. Upgrade Jest and `@types/jest`, keeping `ts-jest@29.4.12` and TypeScript 6 for compatibility.
5. Migrate Tailwind separately because it changes the CSS pipeline and conflicts with the current Sass entry file.
6. Upgrade the remaining runtime libraries and run PDF, feedback email/GitHub issue, dropdown keyboard, translation, persistence, and API validation smoke tests.
